//Needs the following task:
//	*	doBuild:	Builds all artifacts.
//		Remember to distinguish debug/release!
task doBuild(dependsOn:[":closed-source:game:client:doBuild",":closed-source:game:server:doBuild"]) {
	doLast {
		//TODO
	}
}

//	*	doTest:	Tests all artifacts.
//		Remember to distinguish debug/release!
task doTest(dependsOn:[":closed-source:game:client:doTest",":closed-source:game:server:doTest"]) {
	doLast {
		//TODO
	}
}

//	*	doPackage: Packs all artifacts into an archive/installer.
//		Result must be one artifact that can
//		ideally be installed without interaction.
task doPackage(dependsOn:[":closed-source:game:client:doPackage",":closed-source:game:server:doPackage"]) {
	doLast {
		//TODO
	}
}