/*!
 * Defines the SubpassDependency struct.
 */

/**
 * Indicates what subpass connects to what,
 * what part of the pipeline the two sides
 * of the connection relate to,
 * and the access permissions each subpass has
 * in relation to the other.
 */
pub struct SubpassDependency {
	
}