//	*	doBuild:	Builds all artifacts.
//		Remember to distinguish debug/release!
task doBuild(dependsOn:[":open-source:engine:modules:doBuild",":open-source:engine:tools:doBuild"]) {
	doLast {
		//TODO
	}
}

//	*	doTest:	Tests all artifacts.
//		Remember to distinguish debug/release!
task doTest(dependsOn:[":open-source:engine:modules:doTest",":open-source:engine:tools:doTest"]) {
	doLast {
		//TODO
	}
}

//	*	doPackage: Packs all artifacts into an archive/installer.
//		Result must be one artifact that can
//		ideally be installed without interaction.
task doPackage(dependsOn:[":open-source:engine:modules:doPackage",":open-source:engine:tools:doPackage"]) {
	doLast {
		//TODO
	}
}